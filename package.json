{
  "name": "jenkins-option-finder",
  "private": true,
  "version": "0.1.0",
  "description": "Lightweight finder UI for Jenkins git parameter select options",
  "scripts": {
    "test": "node --test ./test/*.test.js",
    "package:safari": "mkdir -p ./dist && zip -r ./dist/jenkins-option-finder-safari-web-extension.zip ./safari-web-extension"
  }
}
